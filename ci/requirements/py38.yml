name: regionmask-tests
channels:
  - conda-forge
dependencies:
  - python=3.8
  - cartopy
  - scipy # optional dependency of cartopy (requred for plotting)
  - geopandas
  - xarray
  - numpy
  - matplotlib-base
  - netcdf4
  - pooch
  - cftime
  - rasterio
  - pytest
  - pytest-cov
  - pytest-env
  - isort
