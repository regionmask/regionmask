name: regionmask-tests
channels:
  - conda-forge
  - nodefaults
dependencies:
  - python=3.8
  - cartopy
  - geopandas
  - matplotlib-base
  - numpy
  - pooch
  - rasterio
  - xarray
# for testing
  - pytest
  - pytest-cov
  - pytest-env
