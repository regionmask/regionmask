name: regionmask-tests
channels:
  - conda-forge
  - nodefaults
dependencies:
  - python=3.6
  - geopandas
  - numpy
  - pooch
  - rasterio
  - setuptools
  - xarray<0.19.0
# for testing
  - pytest
  - pytest-cov
  - pytest-env
  - pytest-timeout
