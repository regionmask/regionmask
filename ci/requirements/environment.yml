name: regionmask-tests
channels:
  - conda-forge
  - nodefaults
dependencies:
  - cartopy
  - geopandas
  - matplotlib-base
  - numpy
  - pooch
  - pygeos
  - rasterio
  - setuptools
  - xarray
  - netcdf4
# for testing
  - pytest
  - pytest-cov
  - pytest-env
  - pytest-timeout
