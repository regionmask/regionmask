name: regionmask-tests
channels:
  - conda-forge
  - nodefaults
dependencies:
  - cartopy
  - geopandas
  - matplotlib-base
  - numpy
  - pooch
  - rasterio
  - setuptools
  - xarray
# for testing
  - pytest
  - pytest-cov
  - pytest-env
