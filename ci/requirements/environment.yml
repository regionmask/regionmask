name: regionmask-tests
channels:
  - conda-forge
  - nodefaults
dependencies:
  - cartopy
  - geopandas
  - matplotlib-base
  - numpy
  - pooch
  - rasterio
  - xarray
# for testing
  - pytest
  - pytest-cov
  - pytest-env
# for linting TODO: remove?
  - black
  - flake8
  - isort
