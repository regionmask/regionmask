name: regionmask-tests
channels:
  - conda-forge
  - nodefaults
dependencies:
  - cartopy
  - geopandas
  - matplotlib-base
  - numpy
  - packaging
  - pooch
  - rasterio
  - shapely<2
  - xarray
# for testing
  - pytest
  - pytest-cov
  - pytest-timeout
