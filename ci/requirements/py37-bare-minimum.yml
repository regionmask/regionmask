name: regionmask-tests
channels:
  - conda-forge
  - nodefaults
dependencies:
  - python=3.7
  - geopandas
  - numpy
  - pooch
  - rasterio
  - setuptools
  - xarray
# for testing
  - pytest
  - pytest-cov
  - pytest-env
  - pytest-timeout
