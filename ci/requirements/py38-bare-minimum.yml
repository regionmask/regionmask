name: regionmask-tests
channels:
  - conda-forge
  - nodefaults
dependencies:
  - python=3.8
  - geopandas
  - numpy
  - packaging
  - pooch
  - rasterio
  - setuptools
  - xarray
# for testing
  - pytest
  - pytest-cov
  - pytest-timeout
