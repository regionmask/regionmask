name: regionmask-tests
channels:
  - conda-forge
  - nodefaults
dependencies:
  - python=3.6
  - cartopy
  - geopandas
  - xarray
  - numpy
  - matplotlib-base
  - netcdf4
  - pooch
  - cftime
  - rasterio
  - pytest
  - pytest-cov
  - pytest-env
