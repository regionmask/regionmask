name: regionmask-tests
channels:
  - conda-forge
  - nodefaults
dependencies:
  - python=3.10
  - geopandas<1.0 # avoid installing pyogrio in one of the environments
  - numpy
  - packaging
  - pooch
  - rasterio
  - shapely
  - xarray
# for testing
  - pytest
  - pytest-cov
  - pytest-timeout
